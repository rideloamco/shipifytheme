<section id="reviews" class="section-reviews">
  <p class="eyebrow">{{ section.settings.eyebrow | default: 'WHAT THE RIDERS SAY' }}</p>
  <h2 class="section-reviews__heading">{{ section.settings.heading | default: 'From the dirt cult' }}</h2>

  <div class="section-reviews__grid">
    {%- for block in section.blocks -%}
      <div class="review-card" {{ block.shopify_attributes }}>
        <div class="review-card__stars" aria-label="{{ block.settings.rating }} stars">
          {%- for i in (1..block.settings.rating) -%}★{%- endfor -%}
        </div>
        <p class="review-card__body">{{ block.settings.review }}</p>
        <div class="review-card__author">— {{ block.settings.author }}</div>
      </div>
    {%- else -%}
      <div class="review-card">
        <div class="review-card__stars" aria-label="5 stars">★★★★★</div>
        <p class="review-card__body">"Best MTB kit I've owned. Tee keeps me cool on the climbs and the price doesn't mug me off. Sound."</p>
        <div class="review-card__author">— TAYLOR · TWEED VALLEY</div>
      </div>
      <div class="review-card">
        <div class="review-card__stars" aria-label="5 stars">★★★★★</div>
        <p class="review-card__body">"Durable, breathable, looks the part. Can't argue with that."</p>
        <div class="review-card__author">— JORDAN · PEAK DISTRICT</div>
      </div>
      <div class="review-card">
        <div class="review-card__stars" aria-label="5 stars">★★★★★</div>
        <p class="review-card__body">"Took it down Cwm Carn in pissing rain. Came back muddy. Tee was fine. Mate of mine ordered the same the next day."</p>
        <div class="review-card__author">— SAM · SOUTH WALES</div>
      </div>
    {%- endfor -%}
  </div>
</section>

{% schema %}
{
  "name": "Reviews",
  "settings": [
    { "type": "text", "id": "eyebrow", "label": "Eyebrow", "default": "WHAT THE RIDERS SAY" },
    { "type": "text", "id": "heading", "label": "Heading", "default": "From the dirt cult" }
  ],
  "blocks": [
    {
      "type": "review",
      "name": "Review",
      "settings": [
        { "type": "range", "id": "rating", "label": "Rating", "min": 1, "max": 5, "step": 1, "default": 5 },
        { "type": "textarea", "id": "review", "label": "Review text", "default": "Great kit, well priced, performs on the trails." },
        { "type": "text", "id": "author", "label": "Author + location", "default": "RIDER · LOCATION" }
      ]
    }
  ],
  "presets": [
    {
      "name": "Reviews",
      "blocks": [
        { "type": "review", "settings": { "rating": 5, "review": "\"Best MTB kit I've owned. Tee keeps me cool on the climbs and the price doesn't mug me off. Sound.\"", "author": "TAYLOR · TWEED VALLEY" } },
        { "type": "review", "settings": { "rating": 5, "review": "\"Durable, breathable, looks the part. Can't argue with that.\"", "author": "JORDAN · PEAK DISTRICT" } },
        { "type": "review", "settings": { "rating": 5, "review": "\"Took it down Cwm Carn in pissing rain. Came back muddy. Tee was fine. Mate of mine ordered the same the next day.\"", "author": "SAM · SOUTH WALES" } }
      ]
    }
  ]
}
{% endschema %}
